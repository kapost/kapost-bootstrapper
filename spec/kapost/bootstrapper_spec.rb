require 'spec_helper'

describe Kapost::Bootstrapper do
  it 'has a version number' do
    expect(Kapost::Bootstrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
